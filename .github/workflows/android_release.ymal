name: Android Release

on:
  # This allows you to click a "Run" button manually
  workflow_dispatch:

  # Keep the tag trigger for later
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build APK & Release
    # ... (The rest of the file stays exactly the same) ...